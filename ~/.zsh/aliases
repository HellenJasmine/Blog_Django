alias dockerstopall = 'docker stop $(docker ps -a -q) ; docker system prune -f ; docker rm -vf $(docker ps -aq) ;'
alias dockerremoveall = 'docker stop $(docker ps -a -q) ; docker system prune -f ; docker rm -vf $(docker ps -aq) ; docker rmi -f $(docker images -aq)'